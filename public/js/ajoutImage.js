
/*$(function () {
    var formElement = $('.imagePrestataire');
    var btnAjout    = $('#ajoutImage');

    //si click sur le bouton d'ajout d'image ajout d un input de type file avec les  class de l'input d'origine
    btnAjout.click(function () {
        //creer un div avec une class divBlock,photo et userItem
        var divParent = $('<div class="divBlock photo userItem"></div>');
        var divElt = $('<div class="form-group"></div>');
        var labelElt = $('<label for="image_photo" class="required">Telecharger une photo</label>');
        var divCustom = $('<div class="custom-file"></div>');


        var input = $('<input type="file" id="image_photo" name="image[photo]" class="custom-file-input"  required="required" lang="en"/>');

        divElt.append(labelElt);
        divCustom.append(input);
        divElt.append(divCustom);
        divParent.append(divElt);
        formElement.append(divParent);


    }
    );

})*/